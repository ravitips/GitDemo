trigger:
- dev  # or your branch name, e.g. dev

pool:
  name: Default  # your self-hosted agent pool name

steps:
- script: |
    echo "Hello from Azure Pipeline!"
    pwd
    ls -l
    whoami
  displayName: 'Run simple shell commands'
